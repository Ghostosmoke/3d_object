# 3D Sine Wave Knot: Процедурная генерация и анимация в Ursina

Этот проект представляет собой демонстрацию возможностей движка **Ursina Engine** для создания процедурной 3D-графики на Python. В отличие от большинства игр, где 3D-модели загружаются из файлов (`.obj`, `.fbx`), здесь сложная геометрическая фигура (Тор-узел) **генерируется математически в реальном времени** через код.

## 📖 Оглавление

1. [О проекте](#о-проекте)
2. [Зачем это нужно?](#зачем-это-нужно)
3. [Требования и установка](#требования-и-установка)
4. [Запуск](#запуск)
5. [Управление](#управление)
6. [Подробный разбор кода](#подробный-разбор-кода)
    - [Математика Тор-узла](#математика-тор-узла)
    - [Построение меша (Mesh)](#построение-меша-mesh)
    - [Освещение и шейдеры](#освещение-и-шейдеры)
    - [Анимация и физика времени](#анимация-и-физика-времени)
7. [Как изменить проект под себя](#как-изменить-проект-под-себя)
8. [Возможные проблемы](#возможные-проблемы)

---

## 🎯 О проекте

Скрипт создает сцену, в которой:
1.  **Процедурно генерируется** сложная замкнутая кривая (Тор-узел типа 2:3), "одетая" в трубку.
2.  Фигура **анимируется**: движется вперед, колеблется по синусоиде вверх-вниз и вращается.
3.  Реализовано **динамическое освещение** с тенями для подчеркивания объема.
4.  Предоставлен **полный контроль камеры** для инспекции объекта.

## 🤔 Зачем это нужно?

Этот проект — не просто визуализация, а образовательный инструмент. Вот почему такие задачи важны:

1.  **Понимание 3D-графики "изнутри"**:
    *   Большинство новичков просто импортируют модели. Здесь вы видите, что любая 3D-модель — это просто список координат (вершин) и список связей между ними (треугольников).
    *   Вы учитесь работать с векторной математикой (нормали, касательные, бинормали).

2.  **Процедурная генерация**:
    *   Позволяет создавать бесконечное разнообразие форм без участия 3D-моделлеров. Это основа для генерации ландшафтов, лабиринтов или абстрактного искусства в играх.

3.  **Визуализация математики**:
    *   Параметрические уравнения (синусы и косинусы) оживают. Вы видите, как формула превращается в физический объект.

4.  **Прототипирование на Python**:
    *   Ursina позволяет быстро проверять гипотезы о движении, освещении и коллизиях без сложной настройки C++ или Unity/Unreal.

---

## 🛠 Требования и установка

Для работы проекта требуется установленный **Python 3.8+**.

### 1. Установка зависимостей
Откройте терминал (командную строку) и установите необходимые библиотеки:

```bash
pip install ursina numpy
```

*   `ursina`: Игровой движок.
*   `numpy`: Библиотека для быстрых математических вычислений (векторы, матрицы).

### 2. Структура файлов
Создайте файл `main.py` и вставьте в него код, предоставленный в описании задачи.
Структура папки:
```text
my_3d_project/
│
├── main.py          # Основной скрипт
└── README.md        # Этот файл
```

---

## 🚀 Запуск

В терминале, находясь в папке проекта, выполните:

```bash
python main.py
```

Откроется окно приложения. Вы увидите синий узел, летящий над серой плоскостью.

---

## 🎮 Управление

В проекте используется `EditorCamera`, что дает возможности, как в редакторах уровней (Blender, Unity):

| Действие | Кнопка | Описание |
| :--- | :--- | :--- |
| **Вращение камеры** | ЛКМ (Зажать) + Движение мыши | Осмотреть фигуру вокруг |
| **Панорама (Сдвиг)** | ПКМ (Зажать) + Движение мыши | Переместить камеру влево/вправо/вверх/вниз |
| **Зум (Приближение)** | Колесо мыши | Приблизить или отдалить объект |
| **Выход** | `Esc` или закрытие окна | Завершить работу приложения |

---

## 🔍 Подробный разбор кода

Здесь мы разберем ключевые моменты реализации, чтобы вы понимали, **почему** код написан именно так.

### 1. Математика Тор-узла
Функция `knot_func(t)` реализует параметрическое уравнение Тор-узла $(p=2, q=3)$:

$$
\begin{aligned}
x &= (R + r \cdot \cos(q \cdot t)) \cdot \cos(p \cdot t) \\
y &= (R + r \cdot \cos(q \cdot t)) \cdot \sin(p \cdot t) \\
z &= r \cdot \sin(q \cdot t)
\end{aligned}
$$

*   **Зачем:** Это позволяет получить идеальную математическую кривую. В коде `R=2` (большой радиус), а колебания трубки задаются синусами/косинусами.
*   **Параметр `t`:** Изменяется от $0$ до $2\pi$, замыкая кольцо.

### 2. Построение меша (Mesh)
3D-движки не понимают "трубки". Они понимают только **треугольники**.
Функция `generate_torus_knot_mesh` делает следующее:

1.  **Скелет:** Вычисляет точки вдоль основной кривой (`points`).
2.  **Ориентация (Frenet Frame):** Для каждой точки вычисляется:
    *   `tangent` (касательная): куда смотрит кривая.
    *   `normal` и `binormal`: векторы, перпендикулярные движению.
    *   **Зачем:** Чтобы кольцо вершин ("срез трубки") всегда было перпендикулярно направлению кривой. Без этого трубка бы "скрутилась" или сплющилась на поворотах.
3.  **Вершины:** По кругу вокруг центральной точки расставляются вершины (`vertices`).
4.  **Треугольники:** Соседние вершины соединяются в пары треугольников (`triangles`), создавая сплошную поверхность.
5.  **Нормали:** `knot_model.generate_normals()` вычисляет направление "наружу" для каждой грани.
    *   **Зачем:** Без нормалей свет не сможет отражаться, и фигура будет выглядеть плоской (как силуэт).

### 3. Освещение и шейдеры
```python
shader=lit_with_shadows_shader
```
*   **Зачем:** Стандартный цвет (unlit) не реагирует на свет. Шейдер `lit_with_shadows` рассчитывает освещенность каждой точки в зависимости от угла падения света.
*   **PointLight parent=knot:** Свет прикреплен к самому узлу.
    *   **Почему:** Если свет стоит статично, а объект улетает далеко, он попадет в темноту. Прикрепив свет к объекту, мы гарантируем, что блики всегда будут видны на поверхности, подчеркивая рельеф трубки при вращении.

### 4. Анимация и физика времени
В функции `update()`:
```python
speed = 4 * time.dt
```
*   **Зачем `time.dt`:** `dt` (delta time) — это время, прошедшее с прошлого кадра.
*   **Проблема без dt:** На мощном ПК игра пойдет слишком быстро, на слабом — медленно.
*   **Решение:** Умножение на `dt` делает скорость движения независимой от FPS (кадров в секунду).

Движение по синусоиде:
```python
knot.y = np.sin(time_tracker * 0.5) * 3
```
*   `time_tracker`: Накопленное расстояние.
*   `* 0.5`: Частота (как часто идут волны).
*   `* 3`: Амплитуда (высота волны).

---

## 🎨 Как изменить проект под себя

Этот код легко модифицировать. Вот несколько идей:

### 1. Изменить форму узла
Найдите функцию `knot_func` и поменяйте коэффициенты:
```python
# Было (2, 3)
x = (2 + np.cos(3 * t)) * np.cos(2 * t)
# Стало (3, 5) - более сложный узел
x = (2 + np.cos(5 * t)) * np.cos(3 * t)
y = (2 + np.cos(5 * t)) * np.sin(3 * t)
z = np.sin(5 * t)
```

### 2. Изменить цвет и материал
В создании Entity `knot`:
```python
knot = Entity(
    model=knot_model,
    color=color.orange,       # Поменяйте на любой цвет
    texture='brick',          # Можно добавить текстуру
    shader=lit_with_shadows_shader,
    ...
)
```

### 3. Ускорить или замедлить
В функции `update()`:
```python
speed = 10 * time.dt  # Увеличьте 4 на 10 для гонки
```

### 4. Добавить след (Trail)
Можно создавать копию узла каждые 0.1 секунды с уменьшенной прозрачностью, чтобы создать эффект шлейфа.

---

## ⚠️ Возможные проблемы

1.  **Черный экран при запуске:**
    *   Убедитесь, что у вас обновлены драйверы видеокарты.
    *   Попробуйте запустить от имени администратора.
    *   Некоторые интегрированные видеокарты (старые Intel HD) могут не поддерживать шейдеры теней. В таком случае уберите `shader=lit_with_shadows_shader` и `shadows=True`.

2.  **Низкий FPS (тормоза):**
    *   Уменьшите детализацию в `generate_torus_knot_mesh`:
        ```python
        # Было
        generate_torus_knot_mesh(radial_segments=12, tubular_segments=64)
        # Стало (меньше полигонов)
        generate_torus_knot_mesh(radial_segments=8, tubular_segments=32)
        ```

3.  **Ошибка `ModuleNotFoundError`:**
    *   Убедитесь, что вы установили библиотеки в тот же интерпретатор Python, из которого запускаете скрипт. Проверьте командой `pip --version`.

---



---
*Создано с целью демонстрации силы Python в 3D-графике.* 🐍🎲
